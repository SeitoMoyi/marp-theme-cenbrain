---
marp: true
theme: cenbrain
paginate: true
size: 16:9
---

# First Page

You'll notice there's no need for a bunch of `<style>` tags or `<img>` tags at the top.  
All visual design has been extracted into the theme package!

- Just write `theme: cenbrain` at the beginning.
- If using VS Code, make sure to add the current folder as a Custom Theme path in the Marp extension settings (otherwise Marp might not find the CSS).

---

# Second Page

Just start writing your content right away!